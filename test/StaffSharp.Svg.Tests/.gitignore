# Test artifacts (generated on test failure for debugging)
Artifacts/

# Snapshots are checked in as the golden baseline
# SVGs are produced during tests for comparison
# Do NOT ignore Snapshots/*.png
Snapshots/*.svg